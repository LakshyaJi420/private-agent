environment:
  sdk: ^3.4.0   # FIXED: Now uses a REAL existing Dart version

dependencies:
  flutter:
    sdk: flutter
  # ... (keep everything else exactly as you have it)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/app-logo.png"
  # min_sdk_size line REMOVED

flutter:
  uses-material-design: true
  assets:
    - assets/
